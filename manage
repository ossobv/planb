#!/bin/sh
here=$(cd "$(dirname "$0")"; pwd)
admin=$(which django-admin.py django-admin | head -n1)
test -z "$admin" && echo "No django-admin found. Missing env?" >&2 && exit 1

PYTHONPATH=$here DJANGO_SETTINGS_MODULE=settings exec "$admin" "$@"
